<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoyu.mapper.WorkProgressMapper">

    <insert id="insertCommit" parameterType="com.xiaoyu.entity.WorkProgressCommit">
        INSERT INTO work_progress_commit (commit_id, content_id, group_id, user_id, description, commit_time)
        VALUES (#{commitId}, #{contentId}, #{groupId}, #{userId}, #{description}, #{commitTime})
        </insert>

</mapper>